<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/play_now_loading"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#181b23"
    android:focusable="false"
    android:visibility="invisible"
    
     >
     <!--ImageView
        android:id="@+id/play_loading_anim"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:focusable="false"
        android:visibility="visible" 
        android:gravity="center"
        android:background="@anim/play_loading_anim" 
        /-->   
         
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:focusable="false"
        android:gravity="center_horizontal" >

        <!-- ImageView
            android:id="@+id/andimationimageview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="30dp"
            android:layout_below="@id/andimationimageview"
            android:background="@drawable/loading_logo"
            android:focusable="false" /-->

        <ProgressBar
            android:id="@+id/loadingProgressBar"
            style="?android:attr/progressBarStyleLarge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:focusable="false"
            android:visibility="visible" />

      <TextView
            android:id="@+id/play_loading_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/loadingProgressBar"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="30dp"
            android:focusable="false"
            android:text="@string/play_loading_txt"
            android:textColor="#FFFFFF"
            android:textSize="28dp"
            android:visibility="visible" /> 
             
    </RelativeLayout> 
  
</RelativeLayout>